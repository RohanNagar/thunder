database:
  type: dynamodb
  endpoint: test.dynamo.com
  region: test-region-1
  tableName: test-table

passwordHash:
  algorithm: bcrypt

auth:
  type: basic
  keys:
    - application: test-app
      secret: test-secret

properties:
  - name: testProperty
    type: list
