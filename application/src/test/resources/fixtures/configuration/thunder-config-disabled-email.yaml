database:
  type: dynamodb
  endpoint: test.dynamo.com
  region: test-region-1
  tableName: test-table

email:
  type: none

passwordHash:
  algorithm: bcrypt

approvedKeys:
  - application: test-app
    secret: test-secret

properties:
  - name: testProperty
    type: list
