database:
  type: dynamodb
  endpoint: test.dynamo.com
  region: test-region-1
  tableName: test-table

email:
  type: ses
  enabled: false

openApi:
  enabled: false

approvedKeys:
  - application: test-app
    secret: test-secret
