database:
  type: dynamodb
  endpoint: test.dynamo.com
  region: test-region-1
  tableName: test-table

email:
  type: none

openApi:
  enabled: false

auth:
  type: basic
  keys:
    - application: test-app
      secret: test-secret
