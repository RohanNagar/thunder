# Information to access DynamoDB
dynamo-table-name: pilot-users-test

# Approved Application Authentication Credentials
approved-keys:
  - application: lightning
    secret: secret

# Server configuration
server:
    applicationConnectors:
        - type: http
          port: 8080
    adminConnectors:
        - type: http
          port: 8081
    requestLog:
        appenders:
          - type: file
            currentLogFilename: var/log/thunder-requests.log
            archivedLogFilenamePattern: var/log/thunder-requests-%d.log

logging:
  level: INFO
  appenders:
    - type: file
      currentLogFilename: var/log/thunder.log
      archivedLogFilenamePattern: var/log/thunder-%d.log